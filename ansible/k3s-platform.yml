---
# Platform is a convenience wrapper: first reconcile the cluster, then optional
# operator-facing add-ons such as Headlamp. Coin-Ops app install is separate.
- import_playbook: k3s-cluster.yml
- import_playbook: k3s-headlamp.yml
