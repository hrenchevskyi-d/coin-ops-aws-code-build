# History API is stateless; durable state is prepared by DB bootstrap.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.historyApi.name }}
  namespace: {{ .Values.historyApi.namespace }}
  labels:
{{ include "coinops-app.labels" . | indent 4 }}
    app.kubernetes.io/name: {{ .Values.historyApi.name }}
spec:
  replicas: {{ .Values.historyApi.replicas }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Values.historyApi.name }}
  template:
    metadata:
      labels:
{{ include "coinops-app.labels" . | indent 8 }}
        app.kubernetes.io/name: {{ .Values.historyApi.name }}
    spec:
      securityContext:
{{ toYaml .Values.historyApi.podSecurityContext | indent 8 }}
      serviceAccountName: {{ .Values.historyApi.serviceAccountName }}
      imagePullSecrets:
        - name: {{ .Values.imagePullSecretName }}
      containers:
        - name: history-api
          image: {{ .Values.historyApi.image }}
          imagePullPolicy: IfNotPresent
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.secret.name }}
                  key: DATABASE_URL
          ports:
            - name: http
              containerPort: {{ .Values.historyApi.servicePort }}
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          securityContext:
{{ toYaml .Values.historyApi.securityContext | indent 12 }}
          resources:
{{ toYaml .Values.historyApi.resources | indent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir: {}
