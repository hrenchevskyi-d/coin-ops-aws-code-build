# Headlamp Ingress Access

Primary URL: `{{ headlamp_access_scheme }}://{{ k3s_headlamp_ingress_host }}/`

Ingress IP: `{{ headlamp_access_ip }}`

TLS enabled: `{{ k3s_headlamp_tls_enabled | bool }}`

Kubernetes service: `{{ k3s_headlamp_release_name }}`

Kubernetes namespace: `{{ k3s_headlamp_namespace }}`

Terraform can manage `{{ k3s_headlamp_ingress_host }}` in Cloudflare as a DNS-only `A` record pointing at `{{ headlamp_access_ip }}`.

Fallback local hosts entry:

`{{ headlamp_access_ip }} {{ k3s_headlamp_ingress_host }}`

URL to open:

`{{ headlamp_access_scheme }}://{{ k3s_headlamp_ingress_host }}/`
