# Headlamp Access Summary

{% if cloud_runtime_metadata.get('headlamp_tunnel_enabled', false) | bool %}Preferred URL: `https://{{ k3s_headlamp_ingress_host }}/`

Access path: `Cloudflare Tunnel (remote-managed)`

Cloudflare Access login: `GitHub`

The private Headlamp ingress load balancer is no longer used for the primary browser path.
{% elif k3s_headlamp_ingress_enabled | bool %}Preferred URL: `{{ headlamp_access_scheme }}://{{ k3s_headlamp_ingress_host }}/`

Ingress load balancer IP: `{{ headlamp_access_ip }}`
{% else %}Preferred URL: `http://127.0.0.1:{{ k3s_headlamp_local_port }}/`

Cloudflare Tunnel is configured in code but not yet active in runtime metadata. Until Terraform has created the tunnel token and DNS, use the port-forward fallback below.
{% endif %}

Kubeconfig:

`{{ k3s_headlamp_local_artifact_dir }}/kubeconfig-gcp-k3s-tunneled.yaml`

Port-forward script:

`{{ k3s_headlamp_local_artifact_dir }}/headlamp-port-forward.sh`

Fallback script:

`{{ k3s_headlamp_local_artifact_dir }}/headlamp-start.sh`

Fallback access remains available through `kubectl port-forward` if the tunnel is unavailable.
