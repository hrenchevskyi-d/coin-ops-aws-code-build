apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ ingress_endpoint_certificate_name }}
  namespace: {{ ingress_endpoint_namespace }}
spec:
  secretName: {{ ingress_endpoint_tls_secret_name }}
  issuerRef:
    name: {{ ingress_endpoint_cluster_issuer_name }}
    kind: ClusterIssuer
  dnsNames:
    - {{ ingress_endpoint_host }}
