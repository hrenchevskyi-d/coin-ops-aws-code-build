# k3s_kubeconfig_access

Resolves the kubeconfig path used by `kubernetes.core` and Helm-based `k3s` application roles.

The role prefers the native node-local `k3s` kubeconfig and falls back to the operator artifact
generated by `k3s_postcheck` when the node-local file is unavailable.
