---
k3s_acme_cloudflare_cert_manager_namespace: cert-manager
k3s_acme_cloudflare_secret_name: cloudflare-api-token-secret
k3s_acme_cloudflare_cluster_issuer_name: letsencrypt-dns-cloudflare
k3s_acme_cloudflare_directory_url: >-
  {{
    'https://acme-staging-v02.api.letsencrypt.org/directory'
    if certbot_staging else
    'https://acme-v02.api.letsencrypt.org/directory'
  }}

# Keep Cloudflare token task output hidden by default, but allow a temporary
# opt-in debug mode via COINOPS_DEBUG_ACME=true when diagnosing localhost
# execution issues.
k3s_acme_cloudflare_hide_secret_task_output: "{{ not ((lookup('env', 'COINOPS_DEBUG_ACME') | default('false', true)) | bool) }}"

k3s_acme_cloudflare_wait_retries: 6
k3s_acme_cloudflare_wait_delay: 5
