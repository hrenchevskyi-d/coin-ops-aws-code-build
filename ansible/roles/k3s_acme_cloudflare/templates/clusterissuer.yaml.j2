apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ acme_cluster_issuer_name }}
spec:
  acme:
    email: {{ certbot_email }}
    server: {{ acme_directory_url }}
    privateKeySecretRef:
      name: {{ acme_cluster_issuer_name }}-account-key
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef:
              name: {{ acme_cloudflare_secret_name }}
              key: api-token
