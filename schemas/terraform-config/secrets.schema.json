{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://coin-ops.local/schemas/terraform-config/secrets.schema.json",
  "title": "Coin-Ops secret manager settings",
  "description": "Controls whether cloud secret managers are used and which secret objects hold database and application credentials.",
  "type": "object",
  "required": ["secrets"],
  "additionalProperties": false,
  "properties": {
    "secrets": {
      "type": "object",
      "required": ["enabled", "names"],
      "additionalProperties": false,
      "properties": {
        "enabled": { "type": "boolean", "description": "Whether Terraform should manage and consume cloud secret manager resources." },
        "names": {
          "type": "object",
          "required": ["db", "app"],
          "additionalProperties": false,
          "properties": {
            "db": { "type": "string", "minLength": 1, "description": "Secret name for database and broker credentials." },
            "app": { "type": "string", "minLength": 1, "description": "Secret name for application, registry, Cloudflare, and optional integration credentials." }
          }
        }
      }
    }
  }
}
